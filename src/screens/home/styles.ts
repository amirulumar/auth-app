import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
    padding: 10,
  },
  heading: {
    fontWeight: "bold",
    color: "#212121", // Dark gray, good for readability
    fontSize: 24,
    lineHeight: 30, // Slightly larger than font size for better spacing
    marginBottom: 12
  },
});

export default styles;
